export const metadata = {
  title: 'Exchange In-House Flow (Mean)',
  description:
    'Learn how to use the Exchange In-House Flow (Mean) endpoint to measure the average internal Bitcoin transfer value within exchange wallets, helping to analyze exchange-level fund movements.',
}

# Exchange In-House Flow (Mean)

The **Exchange In-House Flow (Mean)** metric tracks the average transaction value of Bitcoin transfers occurring within wallets of the same exchange. {{ className: 'lead' }}

This metric helps identify large internal fund reorganizations, reserve consolidations, or cold wallet management activity by exchanges.

---

## Endpoint

`GET /exchange-inhouse-flow-mean`

**Base URL:**  
`https://api.blocklytics.net/api/public/v1`

---

## Example using filters

<Row>
  <Col>

This example retrieves the mean value of internal Bitcoin flows per day. Adjust parameters to specify the exchange, timeframe, or export format.

<Properties>
  <Property name="exchange" type="string">
    The name of the exchange. Example: <code>binance</code>,{' '}
    <code>coinbase</code>, <code>kraken</code>. Default:{' '}
    <code>all_exchange</code>.
  </Property>
  <Property name="window" type="string">
    Time resolution. Supported values: <code>day</code>, <code>hour</code>,{' '}
    <code>block</code>.
  </Property>
  <Property name="from" type="string">
    Start time in <code>YYYYMMDDTHHMMSS</code> or <code>YYYYMMDD</code>. For{' '}
    <code>window=block</code>, specify block height (e.g., <code>510000</code>).
  </Property>
  <Property name="to" type="string">
    End time in <code>YYYYMMDDTHHMMSS</code> or <code>YYYYMMDD</code>. For{' '}
    <code>window=block</code>, specify block height.
  </Property>
  <Property name="limit" type="integer">
    Maximum number of rows to return (1–100,000). Default: <code>100</code>.
  </Property>
  <Property name="format" type="string">
    Output format. Supported values: <code>json</code>, <code>csv</code>.
    Default: <code>json</code>.
  </Property>
</Properties>

  </Col>
  <Col>

```bash {title="Requesting Exchange In-House Flow (Mean) data"}
curl -G "https://api.blocklytics.net/api/public/v1/exchange-inhouse-flow-mean" \
  -H "x-api-key: YOUR_API_KEY" \
  -d exchange=binance \
  -d window=day \
  -d from=20250101 \
  -d to=20250107 \
  -d limit=2
```

```json {title="Sample JSON response"}
[
  {
    "date": "2025-01-07",
    "flow_mean": 15.42
  },
  {
    "date": "2025-01-06",
    "flow_mean": 12.87
  }
]
```

```bash {title="Exporting as CSV"}
curl -G "https://api.blocklytics.net/api/public/v1/exchange-inhouse-flow-mean" \
  -H "x-api-key: YOUR_API_KEY" \
  -d exchange=coinbase \
  -d window=hour \
  -d from=2025010100 \
  -d to=2025010123 \
  -d format=csv
```

  </Col>
</Row>

---

## Understanding the metric

<Row>
  <Col>

The **Exchange In-House Flow (Mean)** helps identify operational fund movements within exchanges:

<Properties>
  <Property name="flow_total" type="number">
    Total BTC transferred internally within exchange wallets during the period.
  </Property>
  <Property name="flow_mean" type="number">
    Average value of internal transactions within the selected window.
  </Property>
  <Property name="transactions_count_flow" type="integer">
    Number of in-house transactions between exchange wallets.
  </Property>
  <Property name="exchange" type="string">
    Name of the exchange being analyzed.
  </Property>
</Properties>

High in-house flow values may indicate cold storage maintenance or exchange liquidity adjustments. Low activity may signal stability in internal fund structures.

  </Col>
  <Col>

```js {title="Node.js (Express) example"}
// Example Express route using Blocklytics
router.get('/exchange-inhouse-flow-mean', async (req, res) => {
  try {
    const window = req.query.window || 'day'
    const exchange = req.query.exchange || 'all_exchange'
    const from = req.query.from ? `&from=${req.query.from}` : ''
    const to = req.query.to ? `&to=${req.query.to}` : ''
    const limit = req.query.limit ? `&limit=${req.query.limit}` : ''
    const format = req.query.format ? `&format=${req.query.format}` : ''

    const path = `exchange-inhouse-flow-mean?window=${window}&exchange=${exchange}${from}${to}${limit}${format}`
    const data = await blocklyticsFetch(path)

    if (!Array.isArray(data)) {
      return res.status(500).json({ error: 'Unexpected API format', data })
    }

    const formatted = data.map((d) => ({
      date: d.date,
      flow_mean: d.flow_mean,
    }))

    res.json(formatted)
  } catch (err) {
    res.status(500).json({ error: err.message })
  }
})
```

  </Col>
</Row>
